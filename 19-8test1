#include<stdio.h>
#include<conio.h>
void main()
{
int a[50],n,i,j,flag=0;
clrscr();
printf("size\n");
scanf("%d",&n);
for(i=0;i<n;i++)
scanf("%d",&a[i]);
for(i=0;i<n;i++)
{
for(j=i+1;j<n;j++)
{
if(a[i]==a[j])
{
flag=1;
break;
}}}
if(flag==0)
printf("UNIQUE");
else
printf("DUPLICATE");
}
