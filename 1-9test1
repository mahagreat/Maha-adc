#include<stdio.h>
#include<conio.h>
int fact(int n);
void main()
{
int n,c;
printf("enter the value\n");
scanf("%d",&n);
c=fact(n);
if(c==0)
printf("even");
else
printf("odd");
}
int fact(int n)
{
int temp;
temp=n%2;
return temp;
}
